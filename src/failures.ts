export enum Failure {
    npmInstall = 'npm-install',
    npmMissing = 'npm-missing',
    sync = 'cap-sync',
    peer = 'peer-dependencies',
    build = 'native-build',
    alreadyTested = 'already-tested'
}