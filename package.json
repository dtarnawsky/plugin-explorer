{
  "scripts": {
    "inspect": "npx tsc && node out/index.js @capacitor/browser",
    "inspect-all": "npx tsc && node out/index.js all",
    "inspect-failed": "npx tsc && node out/index.js --failed",
    "inspect-new": "npx tsc && node out/index.js --new"
  },
  "devDependencies": {
    "@types/node": "^18.15.5",
    "typescript": "^5.0.2"
  }
}
